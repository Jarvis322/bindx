---
deployment:
  tasks:
    - export DEPLOYPATH=/home/bindx/public_html
    - /bin/rm -rf $DEPLOYPATH/*
    - /bin/cp -R ./* $DEPLOYPATH/
    - /bin/cp -R ./.next $DEPLOYPATH/ || true
    - /bin/cp .env.local $DEPLOYPATH/ || true 